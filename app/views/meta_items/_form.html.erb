<%= form_for(meta_item) do |f| %>
  <% if meta_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meta_item.errors.count, "error") %> prohibited this meta_item from being saved:</h2>

      <ul>
      <% meta_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :sku %>
    <%= f.text_field :sku %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
